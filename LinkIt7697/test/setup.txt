<!-- 
https://www.facebook.com/francefu    2020/2/23 08:00
適用 LinkIt 7697 BlocklyDuino v3

Github上放置五個檔案(blocks.js, javascript.js, toolbox.xml, en.js, zh-hant.js)，
程式碼中有中文註解的部分要修改成自訂積木的設定。
若是要新增數個自訂積木，有中文註解的程式原處複製修改成數個自訂積木設定多次執行。
-->
<script>
	var toolbox = document.getElementById('toolbox');
	
	addScript("https://fustyles.github.io/webduino/LinkIt7697/test/zh-hant.js");  //載入自訂積木繁體語系設定檔(預設繁體語系)
	addScript("https://fustyles.github.io/webduino/LinkIt7697/test/blocks.js");   //載入自訂積木定義檔
	addScript("https://fustyles.github.io/webduino/LinkIt7697/test/javascript.js");   //載入自訂積木轉出程式碼檔
	var xml = $.ajax({url: "https://fustyles.github.io/webduino/LinkIt7697/test/toolbox.xml", async: false}).responseXML;  //讀取自訂積木目錄檔
	toolbox.children["category_external"].append(xml.firstChild);    //設定自訂積木的目錄(toolbox.xml)要掛載的官方主目錄Category ID為"category_external" (main.html)

	var myTimer = setInterval(function(){
		if (document.getElementById('select-lang-en').checked!=document.getElementById('select-lang-zh').checked) {
			clearInterval(myTimer);
			if ((document.getElementById('select-lang-en').checked)) {
				addScript("https://fustyles.github.io/webduino/LinkIt7697/test/en.js");  //載入自訂積木英文語系設定檔覆蓋繁體語系設定
			}
			setTimeout(function(){
				setCategoryName("TEST", Blockly.Msg.TEST_SHOW);  //設定自訂積木Category ID為"TEST"的目錄(toolbox.xml)顯示名稱 = Blockly.Msg.TEST_SHOW (語系.js)
				Blockly.updateToolbox(toolbox);
			}, 2000);
		}
	}, 100); 

	function addScript(url) {
		var s = document.createElement("script");
		s.type = "text/javascript";
		s.src = url;
		$("body").append(s);
	}

	function setCategoryName(myCategoryID, myMsgName) {
		document.getElementById(myCategoryID).setAttribute("name", myMsgName);
	}
</script>
